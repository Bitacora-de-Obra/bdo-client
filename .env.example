# Copia este archivo a `.env.local` y completa los valores por entorno.
# Todas las variables deben llevar el prefijo `VITE_` para que Vite las exponga.

# URL base del backend (incluye `/api`).
VITE_API_URL=http://localhost:4001/api

# Clave de Gemini para las funciones de IA del cliente.
GEMINI_API_KEY=

# Credenciales de CometChat (solo si el chat está habilitado).
VITE_COMETCHAT_APP_ID=
VITE_COMETCHAT_REGION=
